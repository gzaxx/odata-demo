# Aspire + MongoDB + OData + Angular + AG Grid

This is a sample project to quickly integrate AG Grid with OData working together with MongoDB

# Known issues

- OData model builder `EnableCamleCase()` is not working
